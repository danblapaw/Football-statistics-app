# Data Scripts

Scripts for ingesting, cleaning, and transforming football data.

## Structure

- `ingest/` — scripts to pull data from external sources (APIs, CSV, etc.)
- `clean/` — normalization and deduplication scripts
- `load/` — scripts to load processed data into PostgreSQL
